<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.edu.ulima.pm.delylab">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DelyLab"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".OrderSucceessActivity"
            android:exported="true" />
        <activity
            android:name=".ui.login.OrderSucceessActivity"
            android:exported="true"
            android:label="@string/title_activity_order_succeess" />
        <activity
            android:name=".PlaceYourOrderActivity"
            android:exported="true" />
        <activity
            android:name=".RestaurantMenuActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity2"
            android:exported="true" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".Registro"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>